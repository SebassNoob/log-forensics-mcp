{
	"name": "pcap-plugin",
	"version": "0.1.0",
	"private": true,
	"main": "index.js",
	"types": "index.d.ts",
	"scripts": {
		"build": "napi build --platform --release",
		"dev": "napi build --platform --watch",
		"format": "cargo fmt --all",
		"lint": "cargo clippy --all-targets --all-features -- -D warnings"
	},
	"napi": {
		"binaryName": "pcap-plugin"
	},
	"devDependencies": {
		"@napi-rs/cli": "^3"
	}
}
